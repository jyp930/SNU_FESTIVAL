import React from 'react';
import PropTypes from 'prop-types';
import * as S from './styles';

function Apply() {
  return (
    <S.StyledApply>
      ApplyContainer
    </S.StyledApply>
  );
}
export default Apply;

Apply.propTypes = {

};
