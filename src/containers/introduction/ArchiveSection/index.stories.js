import React from 'react';
import ArchiveSection from './index';

export default {
  title: 'containers/introduction/ArchiveSection',
};

export const Default = () => <ArchiveSection isMobile={false} theme={{ windowHeight: 1000 }} />;
