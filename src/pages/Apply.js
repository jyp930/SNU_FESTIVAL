import React from 'react';
import ApplyContainer from '@C/apply/Apply';
import withMountEvent from '@U/hoc/withMountEvent';

function Apply() {
  return (
    <ApplyContainer />
  );
}
export default withMountEvent(Apply);
